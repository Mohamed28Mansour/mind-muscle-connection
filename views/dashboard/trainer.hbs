<h1>Welcome {{user.username}}</h1>

<h2>Plans:</h2>

{{#if user.programs}}
{{#each user.programs}}
  <p>
    <a href="/plans/{{plan._id}}">{{plan.planName}}</a>
  </p>
  <button><a href="/plans/edit/{{plan._id}}"></a>Edit Plan</button>
  <button><a href="/plans/delete/{{plan._id}}"></a>Delete Plan</button>
{{/each}}
{{else}}
  <p>No plans available</p>
{{/if}}

<button><a href="/plans">Create a plan</a></button>